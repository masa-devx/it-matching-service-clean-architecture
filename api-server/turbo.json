{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate": {
      "dependsOn": ["@repo/spec#build"],
      "inputs": [
        "oapi-codegen-company.yaml",
        "oapi-codegen-talent.yaml",
        "../packages/spec/openapi-company.yaml",
        "../packages/spec/openapi-talent.yaml",
        "sqlc.yaml",
        "queries/**",
        "../migrations/ddl/**"
      ],
      "outputs": ["generated/**"]
    },
    "build": {
      "dependsOn": ["generate"],
      "inputs": ["**/*.go", "go.mod", "go.sum"],
      "outputs": ["bin/**"]
    },
    "test": {
      "dependsOn": ["generate"],
      "inputs": ["**/*.go", "go.mod", "go.sum", "../migrations/ddl/**"],
      "env": ["TEST_DATABASE_URL"]
    },
    "lint": {
      "dependsOn": ["generate"],
      "inputs": ["**/*.go", "go.mod", "go.sum", ".golangci.yml"]
    }
  }
}
