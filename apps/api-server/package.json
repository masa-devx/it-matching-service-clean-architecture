{
  "name": "api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o bin/server ./cmd/server",
    "test": "go test ./...",
    "lint": "go vet ./..."
  }
}
