# 開発用イメージ（next dev）。本番用ではない
# ソースは COPY せず compose の volume マウントで注入する。
# 依存は node_modules 専用ボリュームに対して起動時にインストールする
# （ホストの node_modules は macOS 用バイナリを含むためコンテナと共有できない）
FROM node:24

WORKDIR /app

# -H 0.0.0.0: コンテナ外（ホストのブラウザ）からのアクセスを受け付ける
CMD ["sh", "-c", "npm install && npm run dev -- -H 0.0.0.0"]
