{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@repo/spec#build"],
      "inputs": ["orval.config.ts", "../spec/openapi-company.yaml"],
      "outputs": ["company/generated/**"]
    }
  }
}
