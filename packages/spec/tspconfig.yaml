# TypeSpec → OpenAPI 3 の変換設定
# 出力はパッケージ直下の openapi-*.yaml（生成物だがコミットする＝差分でAPI変更が見える）
# output-file はエントリ（company / talent）ごとに CLI の --option で指定する
emit:
  - "@typespec/openapi3"
options:
  "@typespec/openapi3":
    emitter-output-dir: "{project-root}"
    file-type: "yaml"
